# Erlang version
erlang_version=18.2.1

# Elixir version
elixir_version=1.4.0

# Always rebuild from scratch on every deploy?
always_rebuild=true

# A command to run right after compiling the app
post_compile="mix phx.digest"
